apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-frontend
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: 'https://github.com/toannd135/btl-shoeshop-frontend.git'
      targetRevision: HEAD
      path: 'frontend-chart'
      helm:
        valueFiles:
          - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: shoeshop-app
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
